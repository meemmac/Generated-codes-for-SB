{
"title": "Tarjan's Algorithm for Strongly Connected Components",
"subtitle": "Find all strongly connected components (SCCs) in a directed graph",
"time_complexity": "O(V + E)",
"space_complexity": "O(V)",
"category": "Graph Theory / DFS",
"code": [
    "def tarjans_scc(graph):",
    "    index = 0",
    "    stack = []",
    "    indices = [-1] * len(graph)",
    "    lowlink = [-1] * len(graph)",
    "    on_stack = [False] * len(graph)",
    "    sccs = []",
    "",
    "    def strongconnect(v):",
    "        nonlocal index",
    "        indices[v] = index",
    "        lowlink[v] = index",
    "        index += 1",
    "        stack.append(v)",
    "        on_stack[v] = True",
    "",
    "        for w in graph[v]:",
    "            if indices[w] == -1:",
    "                strongconnect(w)",
    "                lowlink[v] = min(lowlink[v], lowlink[w])",
    "            elif on_stack[w]:",
    "                lowlink[v] = min(lowlink[v], indices[w])",
    "",
    "        if lowlink[v] == indices[v]:",
    "            scc = []",
    "            while True:",
    "                w = stack.pop()",
    "                on_stack[w] = False",
    "                scc.append(w)",
    "                if w == v:",
    "                    break",
    "            sccs.append(scc)",
    "",
    "    for v in range(len(graph)):",
    "        if indices[v] == -1:",
    "            strongconnect(v)",
    "",
    "    return sccs"
],
"additional_information": "Tarjan's algorithm finds all strongly connected components of a directed graph using a single DFS traversal. It tracks discovery times and low-link values to detect SCC roots efficiently."
}
