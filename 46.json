{
"title": "Gaussian Elimination",
"subtitle": "Solve a system of linear equations (or compute matrix rank / inverse)",
"time_complexity": "O(n^3)",
"space_complexity": "O(n^2)",
"category": "Linear Algebra / Numerical Methods",
"code": [
    "def gaussian_elimination(A, b):",
    "    n = len(A)",
    "    # Form augmented matrix",
    "    M = [row[:] + [b_i] for row, b_i in zip(A, b)]",
    "    for i in range(n):",
    "        # Partial pivot",
    "        pivot = i",
    "        for r in range(i+1, n):",
    "            if abs(M[r][i]) > abs(M[pivot][i]):",
    "                pivot = r",
    "        M[i], M[pivot] = M[pivot], M[i]",
    "        if abs(M[i][i]) < 1e-12:",
    "            raise ValueError('Singular or nearly singular')",
    "        # Normalize and eliminate",
    "        for j in range(i+1, n):",
    "            factor = M[j][i] / M[i][i]",
    "            for k in range(i, n+1):",
    "                M[j][k] -= factor * M[i][k]",
    "    # Back substitution",
    "    x = [0] * n",
    "    for i in range(n-1, -1, -1):",
    "        s = M[i][n]",
    "        for j in range(i+1, n):",
    "            s -= M[i][j] * x[j]",
    "        x[i] = s / M[i][i]",
    "    return x"
],
"additional_information": "Solve linear systems by forward elimination and back substitution. Use partial pivoting for numerical stability."
}
