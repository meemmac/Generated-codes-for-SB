{
"title": "Matrix Chain Multiplication",
"subtitle": "Find the minimum number of scalar multiplications needed to multiply a chain of matrices",
"time_complexity": "O(N³)",
"space_complexity": "O(N²)",
"category": "Dynamic Programming",
"code": [
    "def matrix_chain_order(p):",
    "    n = len(p) - 1",
    "    dp = [[0] * n for _ in range(n)]",
    "",
    "    for l in range(2, n + 1):",
    "        for i in range(n - l + 1):",
    "            j = i + l - 1",
    "            dp[i][j] = float('inf')",
    "            for k in range(i, j):",
    "                q = dp[i][k] + dp[k+1][j] + p[i]*p[k+1]*p[j+1]",
    "                if q < dp[i][j]:",
    "                    dp[i][j] = q",
    "",
    "    return dp[0][n-1]"
],
"additional_information": "Matrix Chain Multiplication uses DP to find the optimal order to multiply matrices, minimizing the total computation cost."
}
