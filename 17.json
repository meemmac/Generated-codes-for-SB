{
"title": "0/1 Knapsack Problem",
"subtitle": "Maximize the total value without exceeding the weight capacity",
"time_complexity": "O(N × W)",
"space_complexity": "O(N × W)",
"category": "Dynamic Programming",
"code": [
    "def knapsack(weights, values, W):",
    "    n = len(weights)",
    "    dp = [[0] * (W + 1) for _ in range(n + 1)]",
    "",
    "    for i in range(1, n + 1):",
    "        for w in range(W + 1):",
    "            if weights[i-1] <= w:",
    "                dp[i][w] = max(dp[i-1][w], dp[i-1][w - weights[i-1]] + values[i-1])",
    "            else:",
    "                dp[i][w] = dp[i-1][w]",
    "",
    "    return dp[n][W]"
],
"additional_information": "The 0/1 Knapsack problem is a classic DP problem where each item can be taken at most once; it helps in resource allocation and optimization scenarios."
}
