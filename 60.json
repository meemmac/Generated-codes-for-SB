{
"title": "Kosaraju's Algorithm",
"subtitle": "Find all strongly connected components (SCCs) in a directed graph",
"time_complexity": "O(V + E)",
"space_complexity": "O(V + E)",
"category": "Graph / SCC",
"code": [
    "def kosaraju(graph):",
    "    n = len(graph)",
    "    visited = [False] * n",
    "    order = []",
    "",
    "    def dfs(u):",
    "        visited[u] = True",
    "        for v in graph[u]:",
    "            if not visited[v]:",
    "                dfs(v)",
    "        order.append(u)",
    "",
    "    for i in range(n):",
    "        if not visited[i]:",
    "            dfs(i)",
    "",
    "    transposed = [[] for _ in range(n)]",
    "    for u in range(n):",
    "        for v in graph[u]:",
    "            transposed[v].append(u)",
    "",
    "    visited = [False] * n",
    "    sccs = []",
    "",
    "    def dfs_transpose(u, component):",
    "        visited[u] = True",
    "        component.append(u)",
    "        for v in transposed[u]:",
    "            if not visited[v]:",
    "                dfs_transpose(v, component)",
    "",
    "    for u in reversed(order):",
    "        if not visited[u]:",
    "            component = []",
    "            dfs_transpose(u, component)",
    "            sccs.append(component)",
    "",
    "    return sccs"
],
"additional_information": "Kosaraju's algorithm finds all strongly connected components using two DFS passes: the first to determine finishing order and the second on the transposed graph."
}
