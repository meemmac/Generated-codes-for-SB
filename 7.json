{
"title": "Floyd–Warshall Algorithm",
"subtitle": "Compute all-pairs shortest paths in a weighted graph",
"time_complexity": "O(V³)",
"space_complexity": "O(V²)",
"category": "Graph Algorithm",
"code": [
    "def floyd_warshall(dist):",
    "    n = len(dist)",
    "    for k in range(n):",
    "        for i in range(n):",
    "            for j in range(n):",
    "                if dist[i][k] + dist[k][j] < dist[i][j]:",
    "                    dist[i][j] = dist[i][k] + dist[k][j]",
    "    return dist"
],
"additional_information": "The Floyd–Warshall algorithm finds shortest paths between all pairs of vertices and can detect negative cycles through the diagonal."
}
