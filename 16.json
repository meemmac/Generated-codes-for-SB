{
"title": "Coin Change (Dynamic Programming)",
"subtitle": "Find the minimum number of coins to make a given amount",
"time_complexity": "O(N × M)",
"space_complexity": "O(N)",
"category": "Dynamic Programming",
"code": [
    "def coin_change(coins, amount):",
    "    dp = [float('inf')] * (amount + 1)",
    "    dp[0] = 0",
    "",
    "    for coin in coins:",
    "        for x in range(coin, amount + 1):",
    "            dp[x] = min(dp[x], dp[x - coin] + 1)",
    "",
    "    return dp[amount] if dp[amount] != float('inf') else -1"
],
"additional_information": "This algorithm computes the minimum coins needed for each sub-amount up to the target amount, building the solution iteratively."
}
