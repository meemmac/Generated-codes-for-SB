{
"title": "Longest Increasing Subsequence (LIS)",
"subtitle": "Find the length of the longest increasing subsequence in an array",
"time_complexity": "O(N²) or O(N log N)",
"space_complexity": "O(N)",
"category": "Dynamic Programming",
"code": [
    "def lis(arr):",
    "    n = len(arr)",
    "    dp = [1] * n",
    "",
    "    for i in range(1, n):",
    "        for j in range(i):",
    "            if arr[i] > arr[j]:",
    "                dp[i] = max(dp[i], dp[j] + 1)",
    "",
    "    return max(dp) if dp else 0"
],
"additional_information": "The LIS problem can also be solved in O(N log N) using patience sorting with binary search, which is optimal for large arrays."
}
