{
"title": "Reservoir Sampling (Algorithm R)",
"subtitle": "Randomly sample k items from a stream of unknown length uniformly",
"time_complexity": "O(n)",
"space_complexity": "O(k)",
"category": "Streaming Algorithms / Randomized",
"code": [
    "import random",
    "def reservoir_sampling(stream, k):",
    "    reservoir = []",
    "    for i, item in enumerate(stream):",
    "        if i < k:",
    "            reservoir.append(item)",
    "        else:",
    "            j = random.randrange(i + 1)",
    "            if j < k:",
    "                reservoir[j] = item",
    "    return reservoir"
],
"additional_information": "Ensures each element in the stream has equal probability k/n of being included in the reservoir when the stream length n is unknown."
}
