{
"title": "Minimum Path Sum in Grid (DP)",
"subtitle": "Find the minimum sum path from top-left to bottom-right in a grid",
"time_complexity": "O(M*N)",
"space_complexity": "O(M*N) or O(N) optimized",
"category": "Dynamic Programming / Grid",
"code": [
    "def min_path_sum(grid):",
    "    if not grid or not grid[0]:",
    "        return 0",
    "    m, n = len(grid), len(grid[0])",
    "    dp = [[0]*n for _ in range(m)]",
    "    dp[0][0] = grid[0][0]",
    "    for i in range(1, m):",
    "        dp[i][0] = dp[i-1][0] + grid[i][0]",
    "    for j in range(1, n):",
    "        dp[0][j] = dp[0][j-1] + grid[0][j]",
    "    for i in range(1, m):",
    "        for j in range(1, n):",
    "            dp[i][j] = grid[i][j] + min(dp[i-1][j], dp[i][j-1])",
    "    return dp[m-1][n-1]"
],
"additional_information": "This classic DP problem calculates the minimum sum path using either the cell above or the cell to the left. Space can be optimized to O(N) using a single row."
}
