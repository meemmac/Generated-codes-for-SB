{
"title": "Stoer–Wagner Global Minimum Cut",
"subtitle": "Find the global minimum cut of an undirected weighted graph",
"time_complexity": "O(V^3) naive or O(V * E + V^2 log V) with heap optimizations",
"space_complexity": "O(V^2) (for adjacency matrix) or O(V + E)",
"category": "Graph Theory / Cuts",
"code": [
    "def stoer_wagner(graph):",
    "    # graph as adjacency matrix or list-of-dicts; here assume matrix",
    "    n = len(graph)",
    "    vertices = list(range(n))",
    "    best = float('inf')",
    "    while len(vertices) > 1:",
    "        used = [False] * n",
    "        weights = [0] * n",
    "        prev = -1",
    "        for _ in range(len(vertices)):",
    "            # select most tightly connected vertex",
    "            sel = -1",
    "            for v in vertices:",
    "                if not used[v] and (sel == -1 or weights[v] > weights[sel]):",
    "                    sel = v",
    "            used[sel] = True",
    "            if _ == len(vertices) - 1:",
    "                # sel is t, prev is s; cut between s and t has weight weights[sel]",
    "                best = min(best, weights[sel])",
    "                # merge s and t",
    "                for v in range(n):",
    "                    graph[prev][v] += graph[sel][v]",
    "                    graph[v][prev] += graph[v][sel]",
    "                vertices.remove(sel)",
    "                break",
    "            prev = sel",
    "            for v in vertices:",
    "                if not used[v]:",
    "                    weights[v] += graph[sel][v]",
    "    return best"
],
"additional_information": "Repeatedly performs 'minimum cut phase' by selecting most tightly connected vertex and merging; returns weight of global minimum cut."
}
